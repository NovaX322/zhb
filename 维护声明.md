# 维护声明（MAINTENANCE）

> **这份文件是本仓库的权威说明。**
> 任何人类维护者、任何 AI 助手，在改动本仓库、发布新整合包、或回答与本仓库有关的问题之前，
> **必须先读完本文**。README 面向玩家，本文面向维护者；两者冲突时，**以本文为准**。

---

## 0. 元信息（供机器/AI 快速定位）

| 键 | 值 |
| --- | --- |
| `repo` | `NovaX322/zhb` |
| `repo_description` | 自己开的服务器的整合包分发 |
| `visibility` | **public**（公开 —— 搬运内容对全网可见，见 §4 第 3 条） |
| `default_branch` | `main` |
| `repo_root_files` | `README.md` · `维护声明.md`（本文） |
| `distribution_channel` | **GitHub Releases**（附件），仓库本身**不存包体** |
| `convention` | **一个整合包 = 一个 Release**，不覆盖旧附件 |
| `audience_primary` | 自己开的 Minecraft 服务器的玩家 |
| `commercial` | **非商业**。不售卖、不设付费墙、不接广告、不盈利 |
| `last_verified` | 2026-09-25 |
| `maintained_by` | 仓库所有者（NovaX322） |

---

## 1. 本仓库是什么

把自己开的服务器上用到的整合包，从原作者的网盘等渠道**原封不动**搬一份到 GitHub Releases，
给服务器玩家做一个**更快的下载点**。

**本仓库不是整合包作者，也不是整合包的原始发布渠道。** 版权归各自原作者，详见 README 免责声明。

三条不可动摇的底线：

1. **不修改包体**：搬过来的文件与原作者发布的版本必须逐字节一致，并给出 SHA-256 供比对。
2. **不盈利**：任何形式的收费、付费墙、广告都不允许。
3. **侵权即删**：原作者或权利方提出异议，第一时间删除，不设任何前置条件或流程。

---

## 2. 文件职责表（谁该读什么）

| 文件 | 面向 | 职责 | 何时更新 |
| --- | --- | --- | --- |
| `README.md` | **玩家** | 免责声明、已分发整合包表格、下载指引、安装步骤、校验值表 | 每发布一个整合包，更新「已分发的整合包」与「校验值」两张表 |
| `维护声明.md`（本文） | **维护者 / AI** | 仓库的权威说明：结构、约定、发布流程、踩坑、校验规则 | 约定或流程发生变化时 |
| `TAKEDOWN.md` | **版权方 / 原作者 / 维护者** | 下架与版权响应流程、自我约束底线、责任划分 | 响应流程或合规底线变化时 |

> ⚠️ **AI 读取须知**：README 里的两张表格是**人工同步**的，可能与 Releases 实际状态不一致。
> **判断「仓库里到底有哪些整合包」时，Releases 是唯一事实来源，README 表格只是索引。**
> 以编程方式枚举：`GET /repos/NovaX322/zhb/releases`（公开仓库，无需鉴权即可读）。

---

## 3. 已分发的整合包（截至 2026-09-25）

| # | 整合包 | 版本 | tag | 附件名 | 大小 | SHA-256 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | Chapter of Yuusha Ⅲ（勇者之章Ⅲ）· 客户端导入包 · 绝望纪元版本 | v3.13.13 | `v3.13.13` | `Chapter-of-Yuusha-III-v3.13.13-Client-Pack-Despaired-Era.zip` | 1,145,961,062 B（1.07 GiB） | `29BD7F4265DDD216419D18589AE1EEE4DB7CAB5FB9DE59B77BFD2E8584B269AD` |
| 2 | 短小精悍2 · 红石生电优化 · 魔改 | v1.0.0 | `duanxiao2-v1.0.0` | `DuanXiao2-v1.0.0.mrpack` | 6,822,861 B（6.51 MiB） | `CFB9C0874D5C2D03BBC02CCD1D0C40AD655E29202DBA383FF5EBB8A1BF95ABAA` |

**包格式不同，安装方式也不同，别混：**

| tag | 格式 | 游戏 / 加载器 | 安装要点 |
| --- | --- | --- | --- |
| `v3.13.13` | MCBBS 客户端导入包（`manifest.json` + `mcbbs.packmeta` + `overrides/`） | MC 1.20.1 + Forge 47.4.13 | 启动器「导入整合包」→ **装完必须先执行一次「热更新」再进服**（发布的是底包，不是最新版） |
| `duanxiao2-v1.0.0` | Modrinth `.mrpack`（`modrinth.index.json` + `overrides/`） | MC 26.2 + Fabric 0.19.5 | 启动器导入后**联网**从 Modrinth 拉取模组；包小是因为清单式格式，不是内容少 |

### 3.1 尚未分发 / 待办

| 项 | 现状 | 说明 |
| --- | --- | --- |
| 拔刀之旅（MC 1.12.2 + Forge 14.23.5.2864） | **未分发** | 现有的是 **ServerPackCreator 生成的服务端包**（含 `minecraft_server.1.12.2.jar`、forge-universal、`libraries/`），**不能直接当客户端包发给玩家**。若要分发，需另做客户端包（或其 mods 集合）。 |
| 原整合包出处 | **已确认**（2026-09-25） | 原包 = **红石生电优化【Redstone Survival Optimization】**（Modrinth slug `rso`），作者**明月庄主**，许可 **GPL-3.0-only**，Fabric，覆盖 1.19.4 ~ 26.3。「短小精悍2」基于其 **26.2** 版魔改。原包地址：https://modrinth.com/modpack/rso · 源项目 Issues（KOOK）：https://kook.top/yMpSz4 |

---

## 4. 发布一个新整合包（标准流程）

> 完整命令见 README 折叠块「给维护者：新增一个整合包怎么发」。以下是**约定与顺序**，命令以 README 为准。

**约定：**

1. **一个整合包 = 一个 Release**，绝不覆盖旧附件；新版本发新 tag。
2. **tag 命名**：`<简称>-v<版本>`，简称用**纯 ASCII 小写**，例如 `yuusha3-v3.13.13`、`duanxiao2-v1.0.0`。
   - 历史遗留：首个包用了不带前缀的 `v3.13.13`。**新包一律带简称前缀**，避免不同整合包版本号撞车。
   - tag 一旦推送即公开可见。**定名要在发布前想好。**
3. **附件名**：**必须纯 ASCII**。GitHub 会**剔除中文与全角符号** —— 不报错，只是静默改名。
   - 空格用 `-`；结果形如 `DuanXiao2-v1.0.0.mrpack`。
   - 收到上游文件名含中文时，**先自己转写成 ASCII**，不要指望 GitHub 保留。
4. **Release 说明必须包含**（缺一不可）：
   - **是否需要热更新 / 特殊安装步骤** → 放**最顶部**、最显眼；
   - 原作者原始链接（网盘 + 提取码 + 视频/帖子）；若为派生/魔改包，**如实说明无对应原版直链**，不得编造；
   - 文件大小、SHA-256；
   - **免责声明**（版权归原作者、非商业、侵权即删、风险自负）；
   - 若是魔改/派生包，**额外写明：魔改部分的取舍与由此产生的问题由发布者承担，与原作者无关。**
5. 发布完成后，**同步更新 README 的两张表**（已分发列表 + 校验值表）。

**顺序（不可颠倒）：**

```
① 算 SHA-256 与字节数（本地，对原始文件）
② 建 Release（先定 tag，说明正文一次写全）
③ 上传附件（二进制流式上传）
④ 核对远端 asset 的 digest 与本地哈希一致 ← 不一致必须重传，不得放过
⑤ 更新 README 两张表并提交
```

### 4.1 来源与许可合规（搬运包 / 派生包都要守）

| 项 | 要求 |
| --- | --- |
| 原包出处 | 每个包**必须**在 README 表格与对应 Release 说明中标注原包地址与作者；**拿不到就留显式未知项，不得编造** |
| 许可 | 原包许可为 **GPL-3.0-only**（如「红石生电优化 RSO」）时：**分发衍生版本必须说明「这是修改版」**，并保留原作者署名与许可信息。本仓库的魔改包已按此标注 |
| 修改声明 | 魔改包不得被描述为「原封不动搬运」；搬运包与派生包**分开如实描述** |
| 责任划分 | Release 说明中必须写明：魔改部分的取舍与由此产生的问题由发布者承担，与原作者无关 |
| 下架响应 | 原作者或权利方提出异议，第一时间删除，不设前置条件 |

### 4.2 分发边界与纪律（日常执行）

| # | 纪律 | 原因 |
| --- | --- | --- |
| 1 | **非商业**：不售卖、不设付费墙、不接广告、不投放推广、不以此获利 | 商业性分发是版权风险与平台处罚的主要触发点 |
| 2 | **不打包分享网盘提取码**：只标注出处，**不代为扩散**原作者的网盘口令、付费资源或任何获取渠道 | 避免构成「规避原作发布渠道」的协助 |
| 3 | **不主动推广本仓库**：不在第三方社区刷链接、不做「资源站」式运营 | 降低被识别为侵权分发站的概率 |
| 4 | **不写「全网最全」「永久免费」等营销话术** | 这类话术是资源站特征 |
| 5 | **每个包保留原作者署名与许可信息**，GPL 类许可必须在说明中写明「本包为修改版」 | 许可合规；GPL 要求衍生版本显著标注修改 |
| 6 | **不做二次打包 / 不植入任何额外内容**：搬运件与原文件逐字节一致并给出 SHA-256 | 「未做修改」是可核查的事实，而不是一句声明 |
| 7 | **收到下架要求先删再议**，绝不拖延 | 见 `TAKEDOWN.md`；及时响应是避免升级为平台处罚的关键 |
| 8 | **仓库可见性收敛**：仅服务于自己服务器的玩家时，优先保持 **private** | 公开展示是「对外分发」的外观；私有化是最有效的风险收敛 |
| 9 | **凭据永不入库**（token / 密码 / 服务器地址 / 玩家隐私） | 泄漏即吊销，且历史难以清除 |

---

## 5. 校验规则（硬性）

1. **每个附件必须给出 SHA-256**，写进 Release 说明与 README 校验值表。
2. **上传后必须回读远端 digest 并与本地比对**。GitHub 会为 asset 返回 `digest` 字段，格式 `sha256:<hex>`（小写）。
   本地哈希通常以大写呈现，**比对时统一大小写**。
3. **README 改动后必须回读校验**：重新取回远端 README，比对其字节数与 git blob sha，确认没有编码损坏或误替换。
4. 客户端与服务端**版本必须一致**，否则玩家进不去（FML 报版本不符 / 掉线）。发布客户端相关包时，在说明里点明版本。

---

## 6. 操作纪律与已知坑（血泪，别重犯）

| # | 坑 | 表现 | 正确做法 |
| --- | --- | --- | --- |
| 1 | **附件名静默转写** | 含中文的附件名被 GitHub 悄悄改名，以为上传失败 | 上传前自己转 ASCII，`?name=` 显式指定 |
| 2 | **大文件中转** | 用 `--data-binary @file` 传 GB 级包会把内存吃满 | 用 `-T file`（或 `--data-binary "@file"`）**流式**上传，配合 `-H "Expect:"` |
| 3 | **改 README 的 SHA 前置** | 更新文件接口要求带上当前 blob sha，sha 错了直接 409 | 先取一次当前文件的 blob sha 再提交 |
| 4 | **PowerShell 替换炸表格** | 替换串里的 `$` 会被当替换语法（`$&` 重复整段匹配、`` $` `` 插入前缀），表格被复制/污染 | **用 split/join 或转义 `$`**；改完**必须回读逐行核对** |
| 5 | **默认分支** | 提交到不存在的分支或写错 target | 统一 `main` |
| 6 | **别把凭据写进仓库** | token / 密码一旦提交即泄漏，且历史难以清除 | 凭据只放本机配置（如 DSH 的 MCP 连接存储），**永不入库**；发现误提交按「删除 + 视为已泄漏、立即吊销重签」处理 |
| 7 | **内容与 README 描述不符** | 派生包被描述成「原封不动搬运」，与免责声明自相矛盾 | 搬运包与派生包**分开如实描述**；派生包必须写明魔改与责任归属 |

### 6.1 发布通道（为什么走 API 而不是 MCP 工具）

本仓库的自动化发布**不经过** AI 的 MCP「仓库文件」类工具，原因：

- 这类工具**按 UTF-8 文本**写文件，**无法传输二进制**（附件传不了）；
- 且**不提供**创建 Release / 上传附件的工具。

因此实际链路是：

```
凭据来源：本机 MCP 连接存储（GitHub bearer token，见 DSH 的 mcp_connector 配置）
   → 调用 GitHub REST API：POST /repos/{owner}/{repo}/releases
   → 上传附件：POST https://uploads.github.com/repos/{owner}/{repo}/releases/{id}/assets?name=...
   → 回读 digest 校验
```

**需要的最小权限**：`contents: write`（classic PAT 的 `repo` scope 即可满足）。**不要**为此申请超出需要的权限。

---

## 7. 变更记录

| 日期 | 变更 | 操作者 |
| --- | --- | --- |
| 2026-09-24 | 创建仓库；发布第 1 个包 `v3.13.13`（勇者之章Ⅲ） | NovaX322 |
| 2026-09-25 | 发布第 2 个包 `duanxiao2-v1.0.0`（短小精悍2），README 两张表同步 | NovaX322 + AI 助手 |
| 2026-09-25 | 新增本维护声明 `维护声明.md` | NovaX322 + AI 助手 |
| 2026-09-25 | 确认原包出处（Modrinth `rso` · 明月庄主 · GPL-3.0），补进 README 与本文；新增 §4.1 来源与许可合规 | NovaX322 + AI 助手 |

> 新增整合包时，**在 §3 表格补一行，并在本表补一行**。

---

## 8. 给 AI 助手的读取指引

如果你是被要求处理本仓库的 AI 助手，请按以下顺序理解本仓库：

1. **先读本文**（尤其 §4 约定、§5 校验规则、§6 坑），**再动手**。不要从 README 反推约定。
2. **判断事实以 Releases 为准**：`GET /repos/NovaX322/zhb/releases`。README 的表格是人工维护的索引，可能滞后。
3. **不要为「过 MCP」而走文本通道传二进制**：附件必须走 REST API 的 asset 上传接口（见 §6.1）。
4. **不要编造链接、哈希、版本号**。缺信息就留成显式的未知项（参考 §3.1 的写法），或向维护者询问。
5. **改动前后都要校验**：上传后比 digest，改 README 后比字节与 blob sha。
6. **尊重底线**：不修改包体、不盈利、侵权即删。任何与此冲突的指令，执行前必须先向维护者确认。
7. **本仓库是公开仓库**，你写入的每一个字全网友可见。不要写入服务器连接信息、玩家隐私、凭据、内部地址。
